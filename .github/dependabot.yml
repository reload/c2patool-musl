# Watches the actions pinned in .github/workflows. It cannot see the images the
# workflows pull with docker run, so bump those by hand.

version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
    commit-message:
      prefix: "Update"
